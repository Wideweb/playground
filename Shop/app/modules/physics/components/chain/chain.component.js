import controller from './chain.ctrl';
import template from './chain.tpl.html';
import './chain.scss';

let chainComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default chainComponent;